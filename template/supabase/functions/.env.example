# Server-only secrets. Never prefixed EXPO_PUBLIC_ — these must not reach the app.
# Generate real random values before deploying:  openssl rand -hex 32
ANTHROPIC_API_KEY=
REVENUECAT_WEBHOOK_AUTH=
# Comma-separated UUIDs of internal accounts. Never an email or a domain.
ADMIN_USER_IDS=
