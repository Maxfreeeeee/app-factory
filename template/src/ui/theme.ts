// One place for the app's visual identity. Change these six values and the
// whole app follows — this is the first thing to edit for a new app, because
// "looks templated" is what the App Store search punishes hardest.
export const palette = {
  bg: "#0B0B0C",
  surface: "#17171A",
  text: "#F5F5F7",
  muted: "#8A8A93",
  accent: "#E8622C",
  danger: "#D9414A",
};

export const spacing = { xs: 4, sm: 8, md: 16, lg: 24, xl: 40 };
export const radius = { sm: 8, md: 14, lg: 22 };
